apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: nginxingresses.operator.ibm.com
spec:
  group: operator.ibm.com
  names:
    kind: NginxIngress
    listKind: NginxIngressList
    plural: nginxingresses
    singular: nginxingress
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      type: object
      x-kubernetes-preserve-unknown-fields: true
      description: nginx ingress is the Schema for the nginx ingress API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: NginxIngressSpec defines the desired state of nginx ingress
          properties:
            fips_enabled:
              description: fips_enabled defines the desired state of fips_enabled
            ingress:
              description: ingress defines the desired state of ingress
            init:
              description: init defines the desired state of init
            defaultBackend:
              description: defaultBackend defines the desired state of defaultBackend
        status:
          description: NginxIngressStatus defines the desired state of nginx ingress
          properties:
            conditions:
              description: conditions defines the desired state of conditions
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
